# GitHub Actions OIDC Authentication

The platform uses GitHub Actions OpenID Connect authentication to securely access AWS resources without long-lived IAM credentials.

Benefits:

- No static AWS secrets
- Short-lived credentials
- Improved CI/CD security posture
